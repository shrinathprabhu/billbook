# Billbook sharing image

Asset: `public/og.png`, 1728 × 910 PNG. The Open Graph and Twitter sharing metadata reference `https://billbook.lowkey.tools/og.png`, with matching width, height and descriptive alt text.

The existing card was edited with the built-in image generation tool and visually checked. The bottom-left credit reads only `by @shrinath_prabhu`, with no website URL or other shoutouts. The app icon and PNG install icons are separate code-native assets.

## Final edit prompt

Use case: text-localization. Asset type: existing Billbook Open Graph sharing card. The supplied image is the edit target. Make one small, localized text edit: replace the bottom-left website text "billbook.lowkey.tools" with exactly "by @shrinath_prabhu". Render the underscore literally, with no backslashes. Match the existing small footer typography, forest-green color, left alignment and baseline. Preserve everything else unchanged: Billbook wordmark, receipt icon, headline "Free offline invoice & receipt generator", supporting line "GST / VAT · Bulk import · PDF & image export", white background, three decorative receipt sheets on the right, spacing, composition and colors. The attribution must be only "by @shrinath_prabhu"; no website URLs, full-name credit, Owleye Analytics shoutout, extra text or watermark. Preserve the original landscape aspect ratio and exact 1728 × 910 dimensions if supported.
