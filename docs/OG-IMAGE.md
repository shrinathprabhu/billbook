# Open Graph image

Asset: `public/og.png`. The sharing card uses the Billbook name and the public URL `lowkey.tools/billbook`. It was edited with the built-in image generation tool and inspected for correct wording. This is an original sharing card, not an application screenshot. Metadata uses the final image’s actual dimensions. The SVG app icon and PNG install icons are separate code-native assets.

## Final edit prompt

Use case: text-localization. Asset type: existing Billbook Open Graph card. Input image is the edit target. Change only the small footer URL at the bottom left so that it reads exactly "lowkey.tools/billbook". Preserve the Billbook title, receipt icon, headline "Free offline invoice & receipt generator", supporting text "GST / VAT · Bulk import · PDF & image export", forest-green typography, white background, decorative paper sheets, typography, alignment, spacing and overall composition. Preserve the landscape aspect ratio and dimensions if possible. No extra text, no watermarks. The sole requested change is the footer URL to lowkey.tools/billbook.
